#!/bin/sh

echo "You can apply some linting rules"
echo "See https://github.com/hadolint/hadolint"
echo ""
echo "You can also parse the Dockerfile..."
echo "Or verify the history of an image by instantiating it and using docker history."
echo "See https://docs.docker.com/engine/reference/commandline/history/"
echo ""
echo "You can collect information and output them in a file (e.g. /tmp/gov.results.txt) before sending them."

